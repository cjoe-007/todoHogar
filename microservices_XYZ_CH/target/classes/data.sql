--Crear tablas
CREATE TABLE client (id INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 100, INCREMENT BY 1) NOT NULL, name VARCHAR(45), email VARCHAR(45), phone VARCHAR(45), benefits VARCHAR(45), groups VARCHAR(45), PRIMARY KEY (ID));
